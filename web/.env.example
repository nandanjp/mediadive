# Copy to web/.env.local for local development: just setup

# Base URL of the mediadive API. Server-only — never exposed to the browser.
API_URL=http://localhost:8080

# Proxy /api/* through Next so browser requests are same-origin locally.
# Production does not set this: Traefik routes /api/* at the ingress instead.
DEV_API_PROXY=1
